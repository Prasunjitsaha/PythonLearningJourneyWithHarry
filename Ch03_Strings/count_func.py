a="I am prasunjit ,I am a python developer, I know automation and backend developement"
print(a.count("I"))  # Counts the occurrences of "I" in the string
print(a.count("am"))  # Counts the occurrences of "am" in the string            
print(a.count("a", 0, 10))  # Counts the occurrences of "a" in the substring from index 0 to 10
print(a.count("a", 10, 20))  # Counts the occurrences of "a" in the substring from index 10 to 20
print(a.count("a", 0, 30))  # Counts the occurrences of "a" in the substring from index 0 to 30